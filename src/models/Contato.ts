class Contact {
  email: string
  id: number
  name: string
  phone: string

  constructor(email: string, id: number, name: string, phone: string) {
    this.email = email
    this.id = id
    this.name = name
    this.phone = phone
  }
}

export default Contact
